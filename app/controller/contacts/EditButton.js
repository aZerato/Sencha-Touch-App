Ext.define('sencha-touch-app.controller.contacts.EditButton', {
    extend: 'Ext.app.Controller',
    require: [
        'sencha-touch-app.view.contacts.EditButton'
    ],

    config: {
        refs: {
			contactEditButton: 'contactEditButton'
        }
    }
});
